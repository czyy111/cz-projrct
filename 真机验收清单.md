# 橙橙计划 Expo Go 真机验收清单

## 验收设备

- Windows 开发电脑
- iPhone 16 Pro Max
- iOS 26.6
- Expo Go
- 项目运行基线：Expo SDK 54（与 iOS App Store 版 Expo Go 匹配）

## 已由自动检查确认

- TypeScript 静态检查通过。
- 8 个测试文件、28 项自动测试通过。
- iOS Hermes 生产导出通过。
- 未写入真实 API Key，未发起真实或收费模型请求。

## 真机必测项目

### 1. 首次使用

- 首次打开依次看到本地数据说明、可跳过的 API 配置、通知说明。
- 不配置 API 也能进入应用并手工创建目标。
- 通知权限只在点击“开启系统通知”后出现。

### 2. 本地数据保持

- 创建一个测试目标和任务，完全关闭 Expo Go 后重新打开，内容仍存在。
- 重启 iPhone 后重新打开，目标、任务和完成状态仍存在。
- 设置页能看到数据与隐私、通知和最近删除入口。

### 3. 每日执行

- 逾期任务与今天任务分区显示。
- 逾期按所属目标分类；今天按时间排列并显示所属目标。
- 完成、撤销、跳过、延后、编辑、删除和恢复后，各页面状态一致。

### 4. 长期习惯

- 连续执行日：漏做或跳过后连续值中断，之后完成从 1 重新开始，最佳值保留。
- 周期次数：只统计当前周或月，达到目标后超出的次数仍记录，但不重复自动播放鼓励。
- 阶段达成后可以选择继续同一习惯、调整、暂停或完成整个目标，不会自动创建下一阶段。

### 5. 鼓励与显示

- 习惯阶段完成出现约 1 秒的橙色鼓励反馈并伴随文字说明。
- 整个目标完成出现更明显的鼓励反馈。
- 在 iOS 开启“减少动态效果”后，反馈降级为淡入或静态确认，功能仍可操作。
- 浅色、深色和大字号下，核心按钮和正文可读且没有关键内容被遮挡。

### 6. 本地通知

- 允许通知后，为未来几分钟创建提醒并等待展示。
- 点击通知正文能进入对应任务。
- 完成、跳过、删除、暂停或改期后，旧提醒不再有效。
- 拒绝通知权限时，任务仍然正常保存。

## Expo Go 阶段不能作为最终结论的项目

- 通知快捷按钮在后台或应用被系统终止后的完整行为。
- 脱离 Expo Go 的独立图标、启动和长期安装。
- 独立应用签名、安装及关机重启后的原生后台行为。

以上项目需以后得到用户确认，再进入独立 iOS 构建阶段验证。
